# Minimal minecraft server 


## usage

Put `server.jar` in the folder with this files<br>

### To start
`docker compose up -d` or `docker-compose up -d`

### To attach
`docker attach <container_name>`
to exit, press ctrl+p+q
